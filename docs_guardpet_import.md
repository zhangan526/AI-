# GuardPet 最新源码导入记录

来源：`GuardPet (1).zip`（2026-09-13）。完整工程已整理到 `GuardPet/`，包含 `app`、`reef`、`appintro` 和 `third_party` 模块。

压缩包中的 Gradle 缓存、构建产物、锁文件和本地配置已排除。源码未发现明文 GitHub Token、API Key 或私钥。

本地验证：在 `GuardPet/` 执行 `./gradlew :app:assembleDebug --offline` 已成功。构建输出仅有现有 Java/Kotlin 弃用警告。
